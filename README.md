# Sunrise Cleaning

Professional cleaning service website built with React and Tailwind CSS.
Features a high-end design with fluid typography, complex GSAP animations, and a responsive layout.

## Getting Started

1. Install dependencies: `npm install`
2. Run development server: `npm run dev`
3. Build for production: `npm run build`
